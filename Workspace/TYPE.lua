--[[ The workspace contains a `{Camera}`, a Baseplate (which contains a `{Decal}`), a `{Terrain}` instance and a `{SpawnLocation}` (which contains another
`{Decal}`) but those are default objects, and camera is just studio's camera (not in-game, that's CurrentCamera), and thus their folders are omitted. ]]--

return "Workspace"
